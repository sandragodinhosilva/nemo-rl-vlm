defaults:
  - sft_vlm_qwen35_4b_video_local_common_megatron.yaml

sft:
  max_num_epochs: 3
  max_num_steps: 1000000
  val_period: 25
  val_batches: 8
  val_global_batch_size: 32
  val_micro_batch_size: 1
  val_at_start: false
  seed: 42

checkpointing:
  enabled: true
  checkpoint_dir: "/mnt/data/sgsilva/checkpoints/sft_qwen35_4b_mcqa_video_3d_2605"
  metric_name: "val:val_loss"
  higher_is_better: false
  keep_top_k: 3
  save_period: 41  # 1324 rows / gbs 32 ~= 42 steps/epoch (no val holdout)
  checkpoint_must_save_by: null

policy:
  # Long-video OOM fix: 0804 has TIER_C trend rows up to 550 frames (~140K
  # tokens, vs 2403's 8-frames-per-rep cap). Mirror the settings that let the
  # 27B human_reps_only config train 65K-token video sequences without OOM.
  # The decisive knob is megatron_cfg.attention_backend: "flash" -- without it
  # TE falls back to the unfused fp32 softmax, which materializes an
  # [heads, S, S] score tensor (the 11.82 GiB allocation that was crashing).
  max_total_sequence_length: 65536
  train_micro_batch_size: 1
  megatron_cfg:
    activation_checkpointing: true
    sequence_parallel: true
    attention_backend: "flash"
    context_parallel_size: 1  # flash + sequence_parallel handles 65K; CP not needed
    optimizer:
      optimizer_cpu_offload: true
      optimizer_offload_fraction: 1.0
    scheduler:
      lr_decay_iters: 123  # 1324 rows / gbs 32 ~= 42 steps/epoch x 3 epochs
      lr_warmup_iters: 13  # ~10% warmup

data:
  _override_: true
  max_input_seq_length: ${policy.max_total_sequence_length}
  add_bos: false
  add_eos: true
  add_generation_prompt: false
  shuffle: true
  num_workers: 16
  seed: 42
  train:
    dataset_name: thrive-vlm-sft
    data_path: /mnt/data/shared/vlm/data/video_aux_datasets/mcqa_video_3d_2605/arrow_train
    split: train
    split_validation_size: 0  # train on ALL rows; no derived 5% val holdout
  validation: null
  default:
    prompt_file: null
    system_prompt_file: null
    processor: "sft_processor"
    skip_missing_local_media_filter: true
    roles_to_train_on: ["assistant"]

# 2-GPU run: base default is 8 GPUs. With tensor_model_parallel_size=2 this is
# one model replica (TP spans both GPUs), data-parallel = 1.
cluster:
  gpus_per_node: 2
  num_nodes: 1

logger:
  log_dir: "logs/sft_qwen35_4b_mcqa_video_3d_2605"
  wandb:
    project: "nemo-rl"
    name: "sft_qwen35_4b_mcqa_video_3d_2605"
  swanlab:
    project: "nemo-rl"
    name: "sft_qwen35_4b_mcqa_video_3d_2605"
  tensorboard:
    log_dir: "logs/sft_qwen35_4b_mcqa_video_3d_2605"
  mlflow:
    experiment_name: "nemo-rl"
    run_name: "sft_qwen35_4b_mcqa_video_3d_2605"
